# La fonction rnorm() permet de créer des vecteurs avec des nombres aléatoires.
monVecteurAlea <- rnorm(45)

# Création d'un vecteur avec chaine de caractère.
monVecteurA <- c("Mut_1", "Mut_2", "Mut_3")

# La fonction rep() permet de répéter une chaine de caractères un certain nombre de fois.
rep("Mut", 4)

# La fonction paste() permet de coller des chaines de caractères en les séparant par un caratères.
paste("Mut", 1, sep = "_")
